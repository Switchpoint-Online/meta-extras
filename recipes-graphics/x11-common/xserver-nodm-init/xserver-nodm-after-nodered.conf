[Unit]
# Start X (and thus the kiosk session) after node-red.service has been started
# by systemd.  The kiosk xsession script still polls port 80 for up to 90 s,
# which covers the time node-red takes to actually start listening.
After=node-red.service
Wants=node-red.service
